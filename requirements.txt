# Essential dependencies
streamlit
PyPDF2
torch==2.1.0  # Specify a stable version of torch
numpy
nltk
faiss-cpu  # Use faiss-gpu if running on a GPU instance

# Transformer & NLP dependencies
transformers==4.36.2  # Ensures compatibility with existing code
sentence-transformers
rank-bm25
langchain
langchain-community

# Fixing version conflicts
pip==25.0.1  # Ensures latest pip to prevent dependency issues

# Additional dependencies (if required for compatibility)
asyncio
